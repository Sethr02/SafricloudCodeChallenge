{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "job-queue",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node ./dist/tests/jobQueue.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}